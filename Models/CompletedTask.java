package Models;

public class CompletedTask extends Task {
    public CompletedTask(String name, double score, double maxScore, TaskType type, String description) {
        super(name, score, maxScore, type, description);
    }
}
